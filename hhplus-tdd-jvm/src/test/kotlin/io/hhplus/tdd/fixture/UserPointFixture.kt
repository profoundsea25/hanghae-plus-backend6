package io.hhplus.tdd.fixture

import io.hhplus.tdd.domain.point.UserPoint

val USER_POINT_1 = UserPoint(1, 10, System.currentTimeMillis())
val USER_POINT_2 = UserPoint(2, 20, System.currentTimeMillis())
val USER_POINT_3 = UserPoint(3, 30, System.currentTimeMillis())
